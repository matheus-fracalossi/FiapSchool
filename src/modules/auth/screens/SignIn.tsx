import {Text} from 'react-native';
import {SafeAreaBackground} from '../../../core/components';

export const SignIn = () => {
  return (
    <SafeAreaBackground>
      <Text>Sign In</Text>
    </SafeAreaBackground>
  );
};
